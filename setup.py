from setuptools import find_packages, setup

setup(
    name="latam-challenge",
    version="0.1.0",
    description="LATAM Airlines ML & AI Engineer challenge",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)